name: SonarCloud Analysis
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.github/**'

jobs:
  sonarcloud-only:
    name: SonarCloud Code Quality Check
    runs-on: ubuntu-latest
    steps:
      # Checkout with full git history for SonarCloud
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Cache SonarCloud scanner (optional optimization)
      - name: Cache SonarCloud packages
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      # Run SonarCloud scan (NO Flutter setup needed!)
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  # Optional: Only include build if you actually need it for other purposes
  # flutter-build:
  #   name: Flutter Build & Test
  #   runs-on: ubuntu-latest
  #   needs: sonarcloud-only
  #   if: success()
  #   steps:
  #     # ... Flutter build steps
